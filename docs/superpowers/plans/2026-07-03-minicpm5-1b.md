# MiniCPM5-1B 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 `aha` 新增 `minicpm5-1b` 文本推理支持，覆盖 Safetensors 与 GGUF 两种加载方式，并打通 CLI `run` / `serv` / 根命令兼容模式以及 OpenAI 风格 chat completion / stream completion。

**架构：** 新建独立 `minicpm5` family，Safetensors 路径复用 `src/models/common::LlamaForCausalLM` 作为标准 Llama 内核，GGUF 路径在 `src/models/minicpm5/model.rs` 内局部实现量化 Llama 装配，避免把通用抽象扩散到 `common`。统一的 `MiniCPM5GenerateModel::init_from_spec(...)` 负责根据 `LoadSpec` 切换 Safetensors / GGUF，两条路径都收敛到同一套 `generate(...)` / `generate_stream(...)` 接口。

**技术栈：** Rust、candle / candle_nn、GGUF metadata/bootstrap、clap、Rocket、aha_openai_dive。

---

## 文件结构

### 新建文件
- `src/models/minicpm5/mod.rs` — family 模块导出。
- `src/models/minicpm5/config.rs` — 反序列化 `config.json`，承载 MiniCPM5 的 Llama 风格文本配置。
- `src/models/minicpm5/model.rs` — MiniCPM5 Safetensors / GGUF 两套底层 runtime 装配与 `forward` / `clear_kv_cache`。
- `src/models/minicpm5/generate.rs` — `LoadSpec` 入口、tokenizer/chat template 初始化、chat completion / stream completion。
- `src/exec/minicpm5.rs` — CLI `run` 子命令直推入口，模式对齐 `src/exec/qwen3_5.rs`。
- `tests/test_minicpm5_multi_format.rs` — Safetensors / GGUF 多格式集成测试（带本地路径 skip 逻辑）。

### 修改文件
- `src/models/mod.rs:1-184,186-388,405-652` — 注册 `pub mod minicpm5;`、新增 `WhichModel::MiniCPM5_1B` 元信息、`ModelInstance::MiniCPM5(...)`、生成分发。
- `src/models/core/artifact.rs:29-148` — 为 `MiniCPM5_1B` 声明 `[Safetensors, Gguf]`，默认 artifact 为 `Safetensors`。
- `src/models/core/registry.rs:3-145` — 新增 `ModelLoaderFamily::MiniCPM5`，使服务端/统一加载路径走 `MiniCPM5GenerateModel::init_from_spec(...)`。
- `src/exec/mod.rs:6-24` — 导出 `pub mod minicpm5;`。
- `src/main.rs:21-97,132-287,416-573,706-819,1052-1287` — CLI 参数解析、`resolve_load_spec_for_*`、`run_target_model_with_spec(...)`、parse tests。
- `tests/test_load_spec.rs:1-130` — `LoadSpec` / artifact 验证覆盖。

### 只读参考（不要改成全局抽象）
- `src/models/common/mod.rs:820-975` — `LlamaForCausalLM` 与 `LlamaModel`，Safetensors 路径直接复用这里。
- `src/models/common/gguf.rs:225-433` — `load_text_bootstrap_from_gguf(...)`、`Gguf`、`QuantizedLinear`、`GateUpDownMLPGguf`。
- `src/models/qwen3/generate.rs:48-214,330-444` — 纯文本多格式 `init_from_spec(...)` 与 `generate` / `generate_stream`。
- `src/models/qwen3_5/model.rs:836-1106` — family 内 GGUF 量化装配、metadata 读取和 `output.weight` 回退模式。
- `src/models/qwen3_5/generate.rs:80-238,417-712` — stream tool-call 标签状态机。
- `src/models/minicpm4/generate.rs:19-144` — 纯文本 stream UTF-8 半截 token 缓冲。
- `src/exec/qwen3_5.rs:14-131` — `run_with_spec(...)` 模式。
- `src/models/glm_ocr/generate.rs:63-173` 与 `src/exec/glm_ocr.rs:64-104` — `init_from_spec(...)` + `run_with_spec(...)` 的项目内标准写法。

## 任务 1：建立 `minicpm5` family 骨架与配置反序列化

**文件：**
- 创建：`src/models/minicpm5/mod.rs`
- 创建：`src/models/minicpm5/config.rs`
- 测试：`src/models/minicpm5/config.rs`

- [ ] **步骤 1：先写失败的配置反序列化单测**

```rust
use candle_nn::Activation;

#[cfg(test)]
mod tests {
    use super::MiniCPM5Config;

    #[test]
    fn minicpm5_config_deserializes_llama_style_fields() {
        let cfg: MiniCPM5Config = serde_json::from_value(serde_json::json!({
            "bos_token_id": 1,
            "eos_token_id": [1, 130073],
            "pad_token_id": 0,
            "hidden_act": "silu",
            "hidden_size": 1536,
            "intermediate_size": 4608,
            "max_position_embeddings": 32768,
            "num_attention_heads": 16,
            "num_hidden_layers": 24,
            "num_key_value_heads": 2,
            "head_dim": 128,
            "rms_norm_eps": 1e-6,
            "rope_theta": 5000000.0,
            "torch_dtype": "bfloat16",
            "tie_word_embeddings": false,
            "use_cache": true,
            "vocab_size": 130560
        }))
        .unwrap();

        assert_eq!(cfg.hidden_size, 1536);
        assert_eq!(cfg.head_dim, 128);
        assert_eq!(cfg.eos_token_id, vec![1, 130073]);
        assert_eq!(cfg.hidden_act, Activation::Silu);
    }
}
```

- [ ] **步骤 2：运行单测确认当前失败**

运行：`cargo test minicpm5_config_deserializes_llama_style_fields -- --exact`

预期：FAIL，编译错误提示 `MiniCPM5Config` / `src::models::minicpm5` 尚不存在。

- [ ] **步骤 3：实现模块骨架与配置结构体**

```rust
// src/models/minicpm5/mod.rs
pub mod config;
pub mod generate;
pub mod model;
```

```rust
// src/models/minicpm5/config.rs
use candle_nn::Activation;

#[derive(Debug, Clone, PartialEq, serde::Deserialize)]
pub struct MiniCPM5Config {
    pub bos_token_id: u32,
    pub eos_token_id: Vec<u32>,
    pub pad_token_id: Option<u32>,
    pub hidden_act: Activation,
    pub hidden_size: usize,
    pub intermediate_size: usize,
    pub max_position_embeddings: usize,
    pub num_attention_heads: usize,
    pub num_hidden_layers: usize,
    pub num_key_value_heads: usize,
    pub head_dim: usize,
    pub rms_norm_eps: f64,
    pub rope_theta: f32,
    pub torch_dtype: String,
    pub tie_word_embeddings: bool,
    pub use_cache: bool,
    pub vocab_size: usize,
}

impl MiniCPM5Config {
    pub fn stop_tokens(&self) -> &[u32] {
        &self.eos_token_id
    }
}
```

- [ ] **步骤 4：重新运行单测确认通过**

运行：`cargo test minicpm5_config_deserializes_llama_style_fields -- --exact`

预期：PASS，输出包含 `test minicpm5_config_deserializes_llama_style_fields ... ok`。

- [ ] **步骤 5：Commit**

```bash
git add src/models/minicpm5/mod.rs src/models/minicpm5/config.rs
git commit -m "feat: add minicpm5 config schema"
```

## 任务 2：注册 `minicpm5-1b` 枚举与 artifact 策略

**文件：**
- 修改：`src/models/mod.rs:1-184,186-388`
- 修改：`src/models/core/artifact.rs:29-148`
- 测试：`tests/test_load_spec.rs`

- [ ] **步骤 1：为 LoadSpec / artifact 先写失败测试**

```rust
#[test]
fn load_spec_minicpm5_auto_resolves_to_safetensors_default() {
    let spec = LoadSpec {
        model: WhichModel::MiniCPM5_1B,
        artifact: ArtifactKind::Auto,
        paths: ModelPaths {
            weight_dir: Some("D:/model_download/MiniCPM5-1B".to_string()),
            ..Default::default()
        },
    };

    assert_eq!(spec.resolved_artifact(), ArtifactKind::Safetensors);
    assert!(spec.validate().is_ok());
}

#[test]
fn load_spec_minicpm5_accepts_gguf() {
    let spec = LoadSpec {
        model: WhichModel::MiniCPM5_1B,
        artifact: ArtifactKind::Gguf,
        paths: ModelPaths {
            gguf_path: Some(
                "D:/model_download/MiniCPM5-1B-GGUF/MiniCPM5-1B-Q4_K_M.gguf".to_string(),
            ),
            ..Default::default()
        },
    };

    assert!(spec.validate().is_ok());
}

#[test]
fn load_spec_minicpm5_rejects_onnx() {
    let spec = LoadSpec {
        model: WhichModel::MiniCPM5_1B,
        artifact: ArtifactKind::Onnx,
        paths: ModelPaths {
            onnx_path: Some("D:/tmp/minicpm5.onnx".to_string()),
            ..Default::default()
        },
    };

    let err = spec.validate().unwrap_err().to_string();
    assert!(err.contains("does not support artifact"));
}
```

- [ ] **步骤 2：运行单测确认失败**

运行：
- `cargo test --test test_load_spec load_spec_minicpm5_auto_resolves_to_safetensors_default -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_accepts_gguf -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_rejects_onnx -- --exact`

预期：FAIL，编译错误提示 `WhichModel::MiniCPM5_1B` 未定义。

- [ ] **步骤 3：补齐模型元信息与 artifact 默认值**

```rust
// src/models/mod.rs
pub mod minicpm5;

#[derive(Debug, Clone, Copy, PartialEq, Eq, clap::ValueEnum)]
pub enum WhichModel {
    #[value(name = "minicpm5-1b", hide = true)]
    MiniCPM5_1B,
    // ...existing variants...
}

pub const LISTED_MODELS: &[WhichModel] = &[
    WhichModel::MiniCPM5_1B,
    // ...existing variants...
];

impl WhichModel {
    pub fn openai_model_id(self) -> &'static str {
        match self {
            WhichModel::MiniCPM5_1B => "minicpm5-1b",
            // ...
        }
    }

    pub fn owner(self) -> &'static str {
        match self {
            WhichModel::MiniCPM5_1B => "OpenBMB",
            // ...
        }
    }

    pub fn model_id(self) -> &'static str {
        match self {
            WhichModel::MiniCPM5_1B => "OpenBMB/MiniCPM5-1B",
            // ...
        }
    }

    pub fn model_type(self) -> &'static str {
        match self {
            WhichModel::MiniCPM5_1B => "llm",
            // ...
        }
    }
}
```

```rust
// src/models/core/artifact.rs
pub fn supported_artifacts(model: WhichModel) -> &'static [ArtifactKind] {
    match model {
        WhichModel::MiniCPM5_1B => &[ArtifactKind::Safetensors, ArtifactKind::Gguf],
        // ...
    }
}

pub fn default_artifact(model: WhichModel) -> ArtifactKind {
    match model {
        WhichModel::MiniCPM5_1B => ArtifactKind::Safetensors,
        // ...
    }
}
```

- [ ] **步骤 4：重新运行 LoadSpec 测试确认通过**

运行：
- `cargo test --test test_load_spec load_spec_minicpm5_auto_resolves_to_safetensors_default -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_accepts_gguf -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_rejects_onnx -- --exact`

预期：PASS，三条用例都输出 `... ok`。

- [ ] **步骤 5：Commit**

```bash
git add src/models/mod.rs src/models/core/artifact.rs tests/test_load_spec.rs
git commit -m "feat: register minicpm5 model metadata"
```

## 任务 3：打通 Safetensors 文本推理与基础 stream

**文件：**
- 创建：`src/models/minicpm5/model.rs`
- 创建：`src/models/minicpm5/generate.rs`
- 测试：`tests/test_minicpm5_multi_format.rs`

- [ ] **步骤 1：先写 Safetensors 集成测试**

```rust
use std::path::Path;

use aha::models::{
    ArtifactKind, GenerateModel, LoadSpec, ModelPaths, WhichModel,
    minicpm5::generate::MiniCPM5GenerateModel,
};
use aha_openai_dive::v1::resources::chat::ChatCompletionParameters;
use anyhow::Result;

const DEFAULT_MINICPM5_SAFETENSORS_DIR: &str = r"D:\model_download\MiniCPM5-1B";

fn build_text_request() -> Result<ChatCompletionParameters> {
    Ok(serde_json::from_value(serde_json::json!({
        "model": "minicpm5-1b",
        "max_tokens": 8,
        "messages": [
            {"role": "user", "content": "请用一句话介绍 Rust。"}
        ]
    }))?)
}

#[test]
fn minicpm5_safetensors_init_from_spec_can_generate() -> Result<()> {
    let weight_dir = std::env::var("AHA_MINICPM5_SAFETENSORS_DIR")
        .unwrap_or_else(|_| DEFAULT_MINICPM5_SAFETENSORS_DIR.to_string());
    if !Path::new(&weight_dir).is_dir() {
        println!(
            "skip safetensors test: dir not found, set AHA_MINICPM5_SAFETENSORS_DIR to run"
        );
        return Ok(());
    }

    let spec = LoadSpec {
        model: WhichModel::MiniCPM5_1B,
        artifact: ArtifactKind::Safetensors,
        paths: ModelPaths {
            weight_dir: Some(weight_dir),
            ..Default::default()
        },
    };
    let mut model = MiniCPM5GenerateModel::init_from_spec(&spec, None, None)?;
    let response = model.generate(build_text_request()?)?;
    let value = serde_json::to_value(response)?;
    let choices_len = value["choices"].as_array().map_or(0, |choices| choices.len());
    assert!(choices_len > 0, "expected at least one generated choice");
    Ok(())
}
```

- [ ] **步骤 2：运行集成测试确认失败**

运行：`cargo test --test test_minicpm5_multi_format minicpm5_safetensors_init_from_spec_can_generate -- --exact`

预期：FAIL，编译错误提示 `minicpm5::generate::MiniCPM5GenerateModel` 尚不存在。

- [ ] **步骤 3：实现 Safetensors 模型装配，直接复用 common Llama 内核**

```rust
// src/models/minicpm5/model.rs
use anyhow::Result;
use candle_core::{DType, Device, Tensor};
use candle_nn::{Embedding, Module, VarBuilder};

use crate::models::{
    common::LlamaForCausalLM,
    minicpm5::config::MiniCPM5Config,
};

pub struct MiniCPM5Model {
    backend: MiniCPM5Backend,
}

enum MiniCPM5Backend {
    Safetensors(LlamaForCausalLM),
    Gguf(Box<MiniCPM5GgufModel>),
}

impl MiniCPM5Model {
    pub fn new_from_vb(vb: VarBuilder, cfg: &MiniCPM5Config) -> Result<Self> {
        let model = LlamaForCausalLM::new(
            vb,
            cfg.vocab_size,
            cfg.hidden_size,
            cfg.num_hidden_layers,
            cfg.num_attention_heads,
            Some(cfg.num_key_value_heads),
            Some(cfg.head_dim),
            false,
            "self_attn",
            Some("o_proj"),
            cfg.intermediate_size,
            cfg.hidden_act,
            false,
            "mlp",
            cfg.rms_norm_eps,
            "input_layernorm",
            "post_attention_layernorm",
            cfg.rope_theta,
        )?;
        Ok(Self {
            backend: MiniCPM5Backend::Safetensors(model),
        })
    }

    pub fn forward(&mut self, input_ids: &Tensor, seqlen_offset: usize) -> Result<Tensor> {
        match &mut self.backend {
            MiniCPM5Backend::Safetensors(model) => {
                let inputs_embeds = model.model.embed_tokens.forward(input_ids)?;
                model.forward(&inputs_embeds, seqlen_offset)
            }
            MiniCPM5Backend::Gguf(model) => model.forward(input_ids, seqlen_offset),
        }
    }

    pub fn clear_kv_cache(&mut self) {
        match &mut self.backend {
            MiniCPM5Backend::Safetensors(model) => model.clear_kv_cache(),
            MiniCPM5Backend::Gguf(model) => model.clear_kv_cache(),
        }
    }
}
```

- [ ] **步骤 4：实现 Safetensors 生成入口与基础 stream**

```rust
// src/models/minicpm5/generate.rs
pub struct MiniCPM5GenerateModel<'a> {
    chat_template: ChatTemplate<'a>,
    tokenizer: TokenizerModel,
    model: MiniCPM5Model,
    device: Device,
    eos_token_ids: Vec<u32>,
    model_name: String,
}

impl<'a> MiniCPM5GenerateModel<'a> {
    pub fn init_from_spec(
        spec: &LoadSpec,
        device: Option<&Device>,
        dtype: Option<DType>,
    ) -> Result<Self> {
        match spec.resolved_artifact() {
            ArtifactKind::Safetensors => {
                let path = spec
                    .paths
                    .weight_dir
                    .as_deref()
                    .ok_or_else(|| anyhow!("weight_path is required for minicpm5 safetensors"))?;
                Self::init(path, device, dtype)
            }
            ArtifactKind::Gguf => {
                let path = spec
                    .paths
                    .gguf_path
                    .as_deref()
                    .ok_or_else(|| anyhow!("gguf_path is required for minicpm5 gguf"))?;
                Self::init_from_gguf(path, device)
            }
            ArtifactKind::Onnx => Err(anyhow!(
                "model {} does not support artifact {:?}",
                spec.model.openai_model_id(),
                ArtifactKind::Onnx
            )),
            ArtifactKind::Auto => unreachable!("artifact kind should be resolved before init"),
        }
    }

    pub fn init(path: &str, device: Option<&Device>, dtype: Option<DType>) -> Result<Self> {
        let chat_template = ChatTemplate::init(path)?;
        let tokenizer = TokenizerModel::init(path)?;
        let cfg: MiniCPM5Config =
            serde_json::from_slice(&std::fs::read(format!("{path}/config.json"))?)?;
        let device = get_device(device);
        let dtype = get_dtype(dtype, cfg.torch_dtype.as_str());
        let model_list = find_type_files(path, "safetensors")?;
        let vb = unsafe { VarBuilder::from_mmaped_safetensors(&model_list, dtype, &device)? };
        let model = MiniCPM5Model::new_from_vb(vb, &cfg)?;
        Ok(Self {
            chat_template,
            tokenizer,
            model,
            device,
            eos_token_ids: cfg.eos_token_id.clone(),
            model_name: "minicpm5-1b".to_string(),
        })
    }
}

impl<'a> GenerateModel for MiniCPM5GenerateModel<'a> {
    fn generate(&mut self, mes: ChatCompletionParameters) -> Result<ChatCompletionResponse> {
        let seed = mes.seed.unwrap_or(34562) as u64;
        let mut logit_processor = get_logit_processor(mes.temperature, mes.top_p, None, seed);
        let mes_render = self.chat_template.apply_chat_template(&mes)?;
        let mut input_ids = self.tokenizer.text_encode(mes_render, &self.device)?;
        let mut seq_len = input_ids.dim(1)?;
        let prompt_tokens = seq_len as u32;
        let mut seqlen_offset = 0usize;
        let mut generated = Vec::new();

        for _ in 0..mes.max_tokens.unwrap_or(1024) {
            let logits = self.model.forward(&input_ids, seqlen_offset)?;
            let logits = logits.squeeze(0)?.squeeze(0)?.to_dtype(DType::F32)?;
            let next_token = logit_processor.sample(&logits)?;
            generated.push(next_token);
            if self.eos_token_ids.contains(&next_token) {
                break;
            }
            seqlen_offset += seq_len;
            seq_len = 1;
            input_ids = Tensor::from_vec(vec![next_token], (1, 1), &self.device)?;
        }

        let text = self.tokenizer.token_decode(generated.clone())?;
        self.model.clear_kv_cache();
        Ok(build_completion_response(
            text,
            &self.model_name,
            Some(generated.len() as u32),
            Some(prompt_tokens),
        ))
    }

    fn generate_stream(
        &mut self,
        mes: ChatCompletionParameters,
    ) -> Result<Box<dyn Stream<Item = Result<ChatCompletionChunkResponse, anyhow::Error>> + Send + Unpin + '_>> {
        let seed = mes.seed.unwrap_or(34562) as u64;
        let mut logit_processor = get_logit_processor(mes.temperature, mes.top_p, None, seed);
        let mes_render = self.chat_template.apply_chat_template(&mes)?;
        let mut input_ids = self.tokenizer.text_encode(mes_render, &self.device)?;
        let mut seq_len = input_ids.dim(1)?;
        let mut seqlen_offset = 0usize;
        let stream = stream! {
            let mut error_tokens = Vec::new();
            for _ in 0..mes.max_tokens.unwrap_or(512) {
                let logits = self.model.forward(&input_ids, seqlen_offset)?;
                let logits = logits.squeeze(0)?.squeeze(0)?.to_dtype(DType::F32)?;
                let next_token = logit_processor.sample(&logits)?;
                let mut decode_ids = error_tokens.clone();
                decode_ids.push(next_token);
                let decoded_token = self.tokenizer
                    .token_decode(decode_ids)
                    .map_err(|e| anyhow!(format!("stream decode error {e}")))?;
                if decoded_token.contains('�') {
                    error_tokens.push(next_token);
                    if error_tokens.len() > 3 {
                        error_tokens.clear();
                    }
                    seqlen_offset += seq_len;
                    seq_len = 1;
                    input_ids = Tensor::from_vec(vec![next_token], (1, 1), &self.device)?;
                    continue;
                }
                error_tokens.clear();
                yield Ok(build_completion_chunk_response(decoded_token, &self.model_name, None, None));
                if self.eos_token_ids.contains(&next_token) {
                    break;
                }
                seqlen_offset += seq_len;
                seq_len = 1;
                input_ids = Tensor::from_vec(vec![next_token], (1, 1), &self.device)?;
            }
            self.model.clear_kv_cache();
        };
        Ok(Box::new(Box::pin(stream)))
    }
}
```

- [ ] **步骤 5：重新运行 Safetensors 集成测试确认通过/skip**

运行：`cargo test --test test_minicpm5_multi_format minicpm5_safetensors_init_from_spec_can_generate -- --exact`

预期：
- 若本地目录存在：PASS，输出包含 `... ok`
- 若目录不存在：PASS，输出包含 `skip safetensors test: dir not found`。

- [ ] **步骤 6：Commit**

```bash
git add src/models/minicpm5/model.rs src/models/minicpm5/generate.rs tests/test_minicpm5_multi_format.rs
git commit -m "feat: add minicpm5 safetensors runtime"
```

## 任务 4：补齐 GGUF 装配与 stream tool-call 标签处理

**文件：**
- 修改：`src/models/minicpm5/model.rs`
- 修改：`src/models/minicpm5/generate.rs`
- 修改：`tests/test_minicpm5_multi_format.rs`
- 测试：`src/models/minicpm5/generate.rs`

- [ ] **步骤 1：先写 GGUF 集成测试与 stream tool-call 单测**

```rust
const DEFAULT_MINICPM5_GGUF_PATH: &str =
    r"D:\model_download\MiniCPM5-1B-GGUF\MiniCPM5-1B-Q4_K_M.gguf";

#[test]
fn minicpm5_gguf_init_from_spec_can_generate() -> Result<()> {
    let gguf_path = std::env::var("AHA_MINICPM5_GGUF_PATH")
        .unwrap_or_else(|_| DEFAULT_MINICPM5_GGUF_PATH.to_string());
    if !Path::new(&gguf_path).is_file() {
        println!("skip gguf test: file not found, set AHA_MINICPM5_GGUF_PATH to run");
        return Ok(());
    }

    let spec = LoadSpec {
        model: WhichModel::MiniCPM5_1B,
        artifact: ArtifactKind::Gguf,
        paths: ModelPaths {
            gguf_path: Some(gguf_path),
            ..Default::default()
        },
    };
    let mut model = MiniCPM5GenerateModel::init_from_spec(&spec, None, None)?;
    let response = model.generate(build_text_request()?)?;
    let value = serde_json::to_value(response)?;
    let choices_len = value["choices"].as_array().map_or(0, |choices| choices.len());
    assert!(choices_len > 0, "expected at least one generated choice");
    Ok(())
}
```

```rust
#[cfg(test)]
mod stream_state_tests {
    use super::MiniCPM5StreamState;

    #[test]
    fn minicpm5_stream_state_buffers_tool_call_until_closing_tag() {
        let mut state = MiniCPM5StreamState::default();
        assert!(state.push("<tool_call>", "minicpm5-1b").unwrap().is_none());
        assert!(state.push("{\"name\":\"lookup\"}", "minicpm5-1b").unwrap().is_none());

        let chunk = state
            .push("</tool_call>", "minicpm5-1b")
            .unwrap()
            .expect("expected chunk on closing tag");
        let payload = serde_json::to_string(&chunk).unwrap();
        assert!(payload.contains("lookup"));
        assert!(payload.contains("tool_calls"));
    }
}
```

- [ ] **步骤 2：运行测试确认失败**

运行：
- `cargo test --test test_minicpm5_multi_format minicpm5_gguf_init_from_spec_can_generate -- --exact`
- `cargo test minicpm5_stream_state_buffers_tool_call_until_closing_tag -- --exact`

预期：FAIL，报错 `init_from_gguf` / `MiniCPM5StreamState` 尚未实现。

- [ ] **步骤 3：在 family 内局部实现 GGUF 量化 Llama 装配**

```rust
use std::io::{Read, Seek};

use anyhow::{Result, anyhow};
use candle_core::{DType, Tensor};
use candle_nn::{Embedding, Module, RmsNorm};

use crate::{
    models::common::{GateUpDownMLPGguf, QuantizedLinear, ProjKind, eager_attention_forward},
    position_embed::rope::{RoPE, apply_rotary_pos_emb},
    utils::tensor_utils::prepare_causal_attention_mask,
};

fn required_u32<R: Read + Seek>(gguf: &crate::models::common::gguf::Gguf<R>, key: &str) -> Result<usize> {
    Ok(gguf
        .get_matedata(key)
        .map_err(|_| anyhow!("missing gguf metadata key: {key}"))?
        .to_u32()? as usize)
}

fn required_f32<R: Read + Seek>(gguf: &crate::models::common::gguf::Gguf<R>, key: &str) -> Result<f32> {
    Ok(gguf
        .get_matedata(key)
        .map_err(|_| anyhow!("missing gguf metadata key: {key}"))?
        .to_f32()?)
}

fn required_string<R: Read + Seek>(gguf: &crate::models::common::gguf::Gguf<R>, key: &str) -> Result<String> {
    Ok(gguf
        .get_matedata(key)
        .map_err(|_| anyhow!("missing gguf metadata key: {key}"))?
        .to_string()?
        .clone())
}

pub struct MiniCPM5GgufAttention {
    q_proj: QuantizedLinear,
    k_proj: QuantizedLinear,
    v_proj: QuantizedLinear,
    o_proj: QuantizedLinear,
    attn_norm: RmsNorm,
    num_heads: usize,
    num_kv_heads: usize,
    num_kv_groups: usize,
    head_dim: usize,
    kv_cache: Option<(Tensor, Tensor)>,
}

impl MiniCPM5GgufAttention {
    fn new_from_gguf<R: Read + Seek>(
        gguf: &mut crate::models::common::gguf::Gguf<R>,
        prefix: &str,
        num_heads: usize,
        num_kv_heads: usize,
        head_dim: usize,
        rms_norm_eps: f64,
    ) -> Result<Self> {
        Ok(Self {
            q_proj: gguf.quantize_linear(&format!("{prefix}.attn_q"), false)?,
            k_proj: gguf.quantize_linear(&format!("{prefix}.attn_k"), false)?,
            v_proj: gguf.quantize_linear(&format!("{prefix}.attn_v"), false)?,
            o_proj: gguf.quantize_linear(&format!("{prefix}.attn_output"), false)?,
            attn_norm: crate::models::common::gguf::Gguf::rms_norm(
                gguf,
                &format!("{prefix}.attn_norm.weight"),
                rms_norm_eps,
            )?,
            num_heads,
            num_kv_heads,
            num_kv_groups: num_heads / num_kv_heads,
            head_dim,
            kv_cache: None,
        })
    }
}

impl MiniCPM5Model {
    pub fn new_from_gguf<R: Read + Seek>(
        gguf: &mut crate::models::common::gguf::Gguf<R>,
        device: &Device,
    ) -> Result<Self> {
        let arch = required_string(gguf, "general.architecture")?;
        let block_count = required_u32(gguf, &format!("{arch}.block_count"))?;
        let num_heads = required_u32(gguf, &format!("{arch}.attention.head_count"))?;
        let num_kv_heads = required_u32(gguf, &format!("{arch}.attention.head_count_kv"))?;
        let head_dim = required_u32(gguf, &format!("{arch}.attention.key_length"))?;
        let hidden_size = required_u32(gguf, &format!("{arch}.embedding_length"))?;
        let intermediate_size = required_u32(gguf, &format!("{arch}.feed_forward_length"))?;
        let rope_theta = required_f32(gguf, &format!("{arch}.rope.freq_base"))?;
        let rms_norm_eps = required_f32(gguf, &format!("{arch}.attention.layer_norm_rms_epsilon"))? as f64;

        let embed_tokens = Embedding::new(gguf.tensor("token_embd.weight")?.dequantize(device)?, hidden_size);
        let mut layers = Vec::with_capacity(block_count);
        for i in 0..block_count {
            let prefix = format!("blk.{i}");
            layers.push(MiniCPM5GgufDecoderLayer::new_from_gguf(
                gguf,
                &prefix,
                num_heads,
                num_kv_heads,
                head_dim,
                intermediate_size,
                rms_norm_eps,
            )?);
        }
        let output_norm = gguf.rms_norm("output_norm.weight", rms_norm_eps)?;
        let lm_head_tensor = match gguf.tensor("output.weight") {
            Ok(tensor) => tensor,
            Err(_) => gguf.tensor("token_embd.weight")?,
        };
        let lm_head = QuantizedLinear::new(candle_core::quantized::QMatMul::from_qtensor(lm_head_tensor)?, None);

        Ok(Self {
            backend: MiniCPM5Backend::Gguf(Box::new(MiniCPM5GgufModel {
                embed_tokens,
                layers,
                output_norm,
                rotary_emb: RoPE::new(head_dim, rope_theta, device)?,
                lm_head,
            })),
        })
    }
}
```

- [ ] **步骤 4：在 generate 层接入 GGUF bootstrap 与 tool-call 状态机**

```rust
#[derive(Default)]
struct MiniCPM5StreamState {
    tool_call_id: Option<String>,
    tool_call_content: String,
}

impl MiniCPM5StreamState {
    fn push(
        &mut self,
        decoded_token: &str,
        model_name: &str,
    ) -> Result<Option<ChatCompletionChunkResponse>> {
        match decoded_token {
            "<tool_call>" => {
                self.tool_call_id = Some(uuid::Uuid::new_v4().to_string());
                self.tool_call_content.clear();
                Ok(None)
            }
            "</tool_call>" => {
                let chunk = build_completion_chunk_response(
                    decoded_token.to_string(),
                    model_name,
                    self.tool_call_id.clone(),
                    Some(self.tool_call_content.clone()),
                );
                self.tool_call_id = None;
                self.tool_call_content.clear();
                Ok(Some(chunk))
            }
            _ if self.tool_call_id.is_some() => {
                self.tool_call_content.push_str(decoded_token);
                Ok(None)
            }
            _ => Ok(Some(build_completion_chunk_response(
                decoded_token.to_string(),
                model_name,
                None,
                None,
            ))),
        }
    }
}

pub fn init_from_gguf(model_file: &str, device: Option<&Device>) -> Result<Self> {
    if !model_file.ends_with(".gguf") {
        return Err(anyhow!("minicpm5 gguf model path must end with .gguf: {model_file}"));
    }

    let device = get_device(device);
    let bootstrap = load_text_bootstrap_from_gguf(model_file, Some(false), Some(false), Some(false))?;
    let chat_template_str = bootstrap
        .chat_template
        .ok_or_else(|| anyhow!("tokenizer.chat_template metadata is missing in {model_file}"))?;
    let mut eos_token_ids = vec![1, 130073];
    if let Some(eos) = bootstrap.eos_token_id
        && !eos_token_ids.contains(&eos)
    {
        eos_token_ids.insert(0, eos);
    }
    let mut gguf = load_gguf_file(model_file, &device)?;
    let model = MiniCPM5Model::new_from_gguf(&mut gguf, &device)?;

    Ok(Self {
        chat_template: ChatTemplate::str_init(&chat_template_str)?,
        tokenizer: bootstrap.tokenizer,
        model,
        device,
        eos_token_ids,
        model_name: std::path::Path::new(model_file)
            .file_stem()
            .and_then(|name| name.to_str())
            .unwrap_or("minicpm5-1b")
            .to_string(),
    })
}
```

把 `generate_stream(...)` 中的普通 chunk 输出替换成：

```rust
let mut state = MiniCPM5StreamState::default();
if let Some(chunk) = state.push(&decoded_token, &model_name)? {
    yield Ok(chunk);
}
```

- [ ] **步骤 5：重新运行 GGUF / tool-call 测试确认通过**

运行：
- `cargo test --test test_minicpm5_multi_format minicpm5_gguf_init_from_spec_can_generate -- --exact`
- `cargo test minicpm5_stream_state_buffers_tool_call_until_closing_tag -- --exact`

预期：
- GGUF 文件存在时：PASS，输出 `... ok`
- GGUF 文件不存在时：PASS，输出 `skip gguf test: file not found...`
- 状态机单测：PASS，输出 `... ok`。

- [ ] **步骤 6：Commit**

```bash
git add src/models/minicpm5/model.rs src/models/minicpm5/generate.rs tests/test_minicpm5_multi_format.rs
git commit -m "feat: add minicpm5 gguf runtime"
```

## 任务 5：接入 registry、ModelInstance、CLI exec 与 `run/serv` 路径

**文件：**
- 修改：`src/models/mod.rs:405-652`
- 修改：`src/models/core/registry.rs:3-145`
- 创建：`src/exec/minicpm5.rs`
- 修改：`src/exec/mod.rs:6-24`
- 修改：`src/main.rs:416-573,706-819`
- 测试：`src/models/core/registry.rs`

- [ ] **步骤 1：先写失败的 registry 路由测试**

```rust
#[test]
fn registry_routes_minicpm5_to_family_loader() {
    assert_eq!(
        resolve_model_loader_family(WhichModel::MiniCPM5_1B),
        ModelLoaderFamily::MiniCPM5
    );
}
```

- [ ] **步骤 2：运行测试确认失败**

运行：`cargo test registry_routes_minicpm5_to_family_loader -- --exact`

预期：FAIL，报错 `MiniCPM5` family / `MiniCPM5_1B` 分支尚未实现。

- [ ] **步骤 3：注册 `ModelInstance`、registry family 和生成分发**

```rust
// src/models/mod.rs
pub enum ModelInstance<'a> {
    MiniCPM5(MiniCPM5GenerateModel<'a>),
    // ...existing variants...
}

impl<'a> GenerateModel for ModelInstance<'a> {
    fn generate(&mut self, mes: ChatCompletionParameters) -> Result<ChatCompletionResponse> {
        match self {
            ModelInstance::MiniCPM5(model) => model.generate(mes),
            // ...
        }
    }

    fn generate_stream(&mut self, mes: ChatCompletionParameters) -> Result<...> {
        match self {
            ModelInstance::MiniCPM5(model) => model.generate_stream(mes),
            // ...
        }
    }
}
```

```rust
// src/models/core/registry.rs
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum ModelLoaderFamily {
    MiniCPM5,
    // ...
}

fn resolve_model_loader_family(model: WhichModel) -> ModelLoaderFamily {
    match model {
        WhichModel::MiniCPM5_1B => ModelLoaderFamily::MiniCPM5,
        // ...
    }
}

pub fn load_model_from_spec<'a>(spec: &LoadSpec) -> Result<ModelInstance<'a>> {
    let model = match resolve_model_loader_family(spec.model) {
        ModelLoaderFamily::MiniCPM5 => {
            ModelInstance::MiniCPM5(MiniCPM5GenerateModel::init_from_spec(spec, None, None)?)
        }
        // ...
    };
    Ok(model)
}
```

- [ ] **步骤 4：增加 `run_with_spec(...)` exec 封装并接入 `main.rs`**

```rust
// src/exec/minicpm5.rs
use anyhow::Result;

use crate::exec::ExecModel;
use crate::models::minicpm5::generate::MiniCPM5GenerateModel;
use crate::models::{GenerateModel, LoadSpec};
use crate::utils::get_file_path;

pub struct MiniCPM5Exec;

impl MiniCPM5Exec {
    pub fn run_with_spec(input: &[String], output: Option<&str>, spec: &LoadSpec) -> Result<()> {
        let input_text = &input[0];
        let target_text = if input_text.starts_with("file://") {
            let path = get_file_path(input_text)?;
            std::fs::read_to_string(path)?
        } else {
            input_text.clone()
        };

        let mut model = MiniCPM5GenerateModel::init_from_spec(spec, None, None)?;
        let request = serde_json::from_value(serde_json::json!({
            "model": "minicpm5-1b",
            "messages": [
                {"role": "user", "content": [{"type": "text", "text": target_text}]}
            ]
        }))?;
        let result = model.generate(request)?;
        println!("Result: {:?}", result);
        if let Some(out) = output {
            std::fs::write(out, format!("{:?}", result))?;
            println!("Output saved to: {}", out);
        }
        Ok(())
    }
}

impl ExecModel for MiniCPM5Exec {
    fn run(input: &[String], output: Option<&str>, weight_path: &str) -> Result<()> {
        let spec = LoadSpec::for_safetensors(crate::models::WhichModel::MiniCPM5_1B, weight_path);
        Self::run_with_spec(input, output, &spec)
    }
}
```

```rust
// src/exec/mod.rs
pub mod minicpm5;
```

```rust
// src/main.rs
fn run_target_model_with_spec(args: &RunArgs, spec: &LoadSpec) -> anyhow::Result<bool> {
    match args.model {
        WhichModel::MiniCPM5_1B => {
            use aha::exec::minicpm5::MiniCPM5Exec;
            MiniCPM5Exec::run_with_spec(&args.input, args.output.as_deref(), spec)?;
            Ok(true)
        }
        // ...
    }
}

fn run_run(args: RunArgs) -> anyhow::Result<()> {
    // legacy fallback branch
    match model {
        WhichModel::MiniCPM5_1B => {
            use aha::exec::minicpm5::MiniCPM5Exec;
            MiniCPM5Exec::run(&input, output.as_deref(), &weight_path)?;
        }
        // ...
    }
}
```

- [ ] **步骤 5：重新运行 registry 单测确认通过**

运行：`cargo test registry_routes_minicpm5_to_family_loader -- --exact`

预期：PASS，输出包含 `... ok`。

- [ ] **步骤 6：Commit**

```bash
git add src/models/mod.rs src/models/core/registry.rs src/exec/mod.rs src/exec/minicpm5.rs src/main.rs
git commit -m "feat: wire minicpm5 family into runtime"
```

## 任务 6：补齐 CLI parse 覆盖与最终多格式回归

**文件：**
- 修改：`src/main.rs:1052-1287`
- 修改：`tests/test_load_spec.rs:1-130`
- 修改：`tests/test_minicpm5_multi_format.rs`

- [ ] **步骤 1：在 `src/main.rs` 里先写失败的 CLI parse tests**

```rust
#[test]
fn parse_run_minicpm5_gguf_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "run",
        "--model",
        "minicpm5-1b",
        "--input",
        "hello",
        "--artifact-format",
        "gguf",
        "--gguf-path",
        "D:\\model_download\\MiniCPM5-1B-GGUF\\MiniCPM5-1B-Q4_K_M.gguf",
    ])
    .expect("run args should parse");

    let Some(Commands::Run(args)) = cli.command else {
        panic!("expected run subcommand");
    };
    assert_eq!(args.model, WhichModel::MiniCPM5_1B);
    assert!(matches!(args.artifact_format, Some(ArtifactArg::Gguf)));
    assert_eq!(
        args.gguf_path.as_deref(),
        Some("D:\\model_download\\MiniCPM5-1B-GGUF\\MiniCPM5-1B-Q4_K_M.gguf")
    );
}

#[test]
fn parse_run_minicpm5_weight_path_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "run",
        "--model",
        "minicpm5-1b",
        "--input",
        "hello",
        "--weight-path",
        "D:\\model_download\\MiniCPM5-1B",
    ])
    .expect("run args should parse");

    let Some(Commands::Run(args)) = cli.command else {
        panic!("expected run subcommand");
    };
    assert_eq!(args.model, WhichModel::MiniCPM5_1B);
    assert_eq!(args.weight_path.as_deref(), Some("D:\\model_download\\MiniCPM5-1B"));
}

#[test]
fn parse_serv_minicpm5_gguf_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "serv",
        "--model",
        "minicpm5-1b",
        "--artifact-format",
        "gguf",
        "--gguf-path",
        "D:\\model_download\\MiniCPM5-1B-GGUF\\MiniCPM5-1B-Q4_K_M.gguf",
    ])
    .expect("serv args should parse");

    let Some(Commands::Serv(args)) = cli.command else {
        panic!("expected serv subcommand");
    };
    assert_eq!(args.common.model, WhichModel::MiniCPM5_1B);
    assert!(matches!(args.artifact_format, Some(ArtifactArg::Gguf)));
}

#[test]
fn parse_backward_compatible_root_minicpm5_gguf_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "--model",
        "minicpm5-1b",
        "--artifact-format",
        "gguf",
        "--gguf-path",
        "D:\\model_download\\MiniCPM5-1B-GGUF\\MiniCPM5-1B-Q4_K_M.gguf",
    ])
    .expect("root args should parse");

    assert!(cli.command.is_none());
    assert_eq!(cli.model, Some(WhichModel::MiniCPM5_1B));
    assert!(matches!(cli.artifact_format, Some(ArtifactArg::Gguf)));
}
```

- [ ] **步骤 2：运行 parse tests 确认失败**

运行：
- `cargo test parse_run_minicpm5_gguf_flags -- --exact`
- `cargo test parse_run_minicpm5_weight_path_flags -- --exact`
- `cargo test parse_serv_minicpm5_gguf_flags -- --exact`
- `cargo test parse_backward_compatible_root_minicpm5_gguf_flags -- --exact`

预期：如果前面漏掉 CLI/route wiring，这里会 FAIL；错误通常是 clap 无法识别 `minicpm5-1b` 或 `run_target_model_with_spec(...)` 未覆盖。

- [ ] **步骤 3：补齐遗漏的 parse / route 细节并运行最终回归**

运行：
- `cargo test parse_run_minicpm5_gguf_flags -- --exact`
- `cargo test parse_run_minicpm5_weight_path_flags -- --exact`
- `cargo test parse_serv_minicpm5_gguf_flags -- --exact`
- `cargo test parse_backward_compatible_root_minicpm5_gguf_flags -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_auto_resolves_to_safetensors_default -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_accepts_gguf -- --exact`
- `cargo test --test test_load_spec load_spec_minicpm5_rejects_onnx -- --exact`
- `cargo test registry_routes_minicpm5_to_family_loader -- --exact`
- `cargo test minicpm5_config_deserializes_llama_style_fields -- --exact`
- `cargo test minicpm5_stream_state_buffers_tool_call_until_closing_tag -- --exact`
- `cargo test --test test_minicpm5_multi_format minicpm5_safetensors_init_from_spec_can_generate -- --exact`
- `cargo test --test test_minicpm5_multi_format minicpm5_gguf_init_from_spec_can_generate -- --exact`

预期：
- 单测全部 PASS。
- 本地模型文件存在时，两条集成测试 PASS。
- 本地模型文件不存在时，集成测试输出 skip 信息并返回 `ok`。

- [ ] **步骤 4：如有失败，按最小改动修正后重跑同一条用例**

```rust
// 只改失败用例打到的最小范围：
// - artifact 选择错误：修 src/models/core/artifact.rs
// - service loader 路由错误：修 src/models/core/registry.rs
// - CLI parse 错误：修 src/main.rs
// - stream tool-call 错误：修 src/models/minicpm5/generate.rs
```

运行：仅重跑刚才失败的那一条 `cargo test ... -- --exact`，直到该条通过，再继续下一条。

预期：每次修复都只影响一个失败点，不引入额外回归。

- [ ] **步骤 5：Commit**

```bash
git add src/main.rs tests/test_load_spec.rs tests/test_minicpm5_multi_format.rs src/models/minicpm5/generate.rs
git commit -m "test: cover minicpm5 multi-format paths"
```

## 交付检查

在声称实现完成前，逐项核对：
- `minicpm5-1b` 出现在 `WhichModel`、`LISTED_MODELS`、`/models` 响应元信息里。
- `supported_artifacts(WhichModel::MiniCPM5_1B)` 返回 `[Safetensors, Gguf]`。
- `default_artifact(WhichModel::MiniCPM5_1B)` 返回 `Safetensors`。
- `registry::load_model_from_spec(...)` 对 `MiniCPM5_1B` 走 family loader，而不是 legacy path。
- `run_target_model_with_spec(...)` 支持 `MiniCPM5_1B`，`run_run(...)` fallback 分支也支持。
- Safetensors 路径复用了 `common::LlamaForCausalLM`，没有把 MiniCPM5 错绑到 `qwen3_5`。
- GGUF 路径在 `src/models/minicpm5/model.rs` 内局部实现，没有改造 `src/models/common/*` 的全局抽象。
- GGUF 缺失 `general.architecture` / `tokenizer.chat_template` / 核心 metadata / 核心张量时，错误信息里包含缺失 key 或 tensor 名。
- 生成终止条件至少覆盖 `1` 和 `130073`。
- stream 路径对 `<tool_call>` / `</tool_call>` 做了标签累积处理；非 stream 路径保持普通文本 completion。

## 非目标提醒

这份计划**不**包含以下内容；实现时不要顺手扩 scope：
- MiniCPM5 图像 / 视频 / 音频输入
- ONNX backend
- 将 GGUF Llama 抽象提升到 `src/models/common`
- 重构现有 `qwen3_5` / `minicpm4` family
- 给 `download` 子命令增加 GGUF 目录自动管理
