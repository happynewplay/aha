# LFM2.5-Embedding-350M 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为 `aha` 新增 `lfm2.5-embedding-350m` 本地 embedding 支持，覆盖 safetensors 加载、CLI `run`、服务端 `/embeddings`、`prompt_name=query|document`、测试与文档。

**架构：** 新建独立 `lfm2_5_embedding` family，按 `sentence-transformers` 目录结构解析 prompt 与 pooling 配置，并在内部实现一个不带 causal mask 的双向 LFM2.5 encoder。统一的 `EmbeddingOptions` 负责把 `prompt_name` 从 CLI 与 API 透传到 `ModelInstance::embedding(&[String], &EmbeddingOptions)`，现有 embedding 模型忽略该选项，新模型按 `query/document` 自动加前缀。

**技术栈：** Rust、candle / candle_nn、Rocket、clap、serde/serde_json、现有 `TokenizerModel` 与 `LoadSpec` 基础设施。

**工作区约束：** 当前仓库策略禁止自动 commit。下面所有任务都将“commit”替换为 diff review 检查点，不执行 `git add` / `git commit`。

---

## 文件结构

### 新建文件
- `src/models/lfm2_5_embedding/mod.rs` — family 模块导出。
- `src/models/lfm2_5_embedding/config.rs` — 读取 `config.json`、`config_sentence_transformers.json`、`modules.json`、`1_Pooling/config.json`，并做结构校验。
- `src/models/lfm2_5_embedding/model.rs` — safetensors backend、prompt 注入、双向 encoder 前向、CLS pooling、L2 normalize。
- `src/models/lfm2_5_embedding/generate.rs` — `LoadSpec` 入口与 `embed_with_options(&[String], EmbeddingOptions)` 封装。
- `src/exec/lfm2_5_embedding.rs` — CLI `run` 入口，读取单条文本或 `file://` 文本文件并输出 embedding JSON。
- `tests/test_lfm2_5_embedding_safetensors.rs` — 本地 safetensors 加载、维度、prompt 差异、语义烟测。

### 修改文件
- `src/models/mod.rs` — 新增 `WhichModel::LFM2_5Embedding350M`、`EmbeddingPromptName`、`EmbeddingOptions`、`ModelInstance::LFM2_5Embedding` 和统一 `embedding(&[String], &EmbeddingOptions)` 分发。
- `src/models/core/artifact.rs` — 为新模型声明只支持 `Safetensors`，默认 artifact 为 `Safetensors`。
- `src/models/core/registry.rs` — 注册 `Lfm2_5EmbeddingModel::init_from_spec(spec, None, None)`。
- `src/exec/mod.rs` — 导出 `pub mod lfm2_5_embedding;`。
- `src/main.rs` — 新增 `--prompt-name` CLI 参数、`run_target_model_with_spec` 分发、新模型 parse tests。
- `src/api/mod.rs` — `/embeddings` 请求增加 `prompt_name`，解析并传给 `ModelInstance::embedding(&[String], &EmbeddingOptions)`。
- `tests/test_load_spec.rs` — `LoadSpec` 对新模型的 safetensors / onnx 校验。
- `README.md` — 增加本地运行与 `/embeddings` 示例。
- `README.zh-CN.md` — 中文示例同步更新。
- `docs/supported-models.md` — 把新模型加入 embedding 列表。
- `docs/supported-models.zh-CN.md` — 中文模型列表同步更新。

### 只读参考
- `src/models/all_minilm_l6_v2/config.rs` — sentence-transformers 风格 pooling / modules 解析模式。
- `src/models/all_minilm_l6_v2/model.rs` — embedding backend 的 tokenizer、pooling、normalize 组织方式。
- `src/models/qwen3_embedding/model.rs` — 统一 `embed_texts(input)` 与 backend 枚举模式。
- `src/models/lfm2_5/model.rs` — LFM2.5 的 conv / attention / MLP 基元与权重命名；只复用思路，不直接套用因果前向。
- `src/exec/all_minilm_l6_v2.rs` — embedding CLI 输出 JSON 的标准写法。
- `src/api/mod.rs` — 当前 `/embeddings` 请求与响应结构。
- `src/main.rs` — `RunArgs`、`run_target_model_with_spec`、CLI parse tests 位置。

## 任务 1：注册新模型 ID 与 artifact 策略

**文件：**
- 修改：`src/models/mod.rs`
- 修改：`src/models/core/artifact.rs`
- 测试：`tests/test_load_spec.rs`

- [ ] **步骤 1：先在 `tests/test_load_spec.rs` 写失败测试**

```rust
#[test]
fn load_spec_lfm2_5_embedding_auto_resolves_to_safetensors_default() {
    let spec = LoadSpec {
        model: WhichModel::LFM2_5Embedding350M,
        artifact: ArtifactKind::Auto,
        paths: ModelPaths {
            weight_dir: Some("D:/model_download/LFM2.5-Embedding-350M".to_string()),
            ..Default::default()
        },
    };

    assert_eq!(spec.resolved_artifact(), ArtifactKind::Safetensors);
    assert!(spec.validate().is_ok());
}

#[test]
fn load_spec_lfm2_5_embedding_rejects_onnx() {
    let spec = LoadSpec {
        model: WhichModel::LFM2_5Embedding350M,
        artifact: ArtifactKind::Onnx,
        paths: ModelPaths {
            onnx_path: Some("D:/model_download/LFM2.5-Embedding-350M-ONNX".to_string()),
            ..Default::default()
        },
    };

    let err = spec.validate().unwrap_err().to_string();
    assert!(err.contains("does not support artifact"));
}
```

- [ ] **步骤 2：运行目标测试确认当前失败**

运行：
```bash
cargo test --test test_load_spec load_spec_lfm2_5_embedding_auto_resolves_to_safetensors_default -- --exact
```

预期：FAIL，编译错误提示 `WhichModel::LFM2_5Embedding350M` 尚不存在。

- [ ] **步骤 3：补齐新模型枚举与 artifact 映射**

```rust
// src/models/mod.rs
#[value(name = "lfm2.5-embedding-350m", hide = true)]
LFM2_5Embedding350M,
```

```rust
// src/models/mod.rs - insert into the existing tables / match arms
WhichModel::LFM2_5Embedding350M,
WhichModel::LFM2_5Embedding350M => "lfm2.5-embedding-350m",
WhichModel::LFM2_5Embedding350M => "LiquidAI",
WhichModel::LFM2_5Embedding350M => "LiquidAI/LFM2.5-Embedding-350M",
WhichModel::LFM2_5Embedding350M => "embedding",
```

```rust
// src/models/core/artifact.rs - add dedicated match arms
WhichModel::LFM2_5Embedding350M => &[ArtifactKind::Safetensors],
WhichModel::LFM2_5Embedding350M => ArtifactKind::Safetensors,
```

- [ ] **步骤 4：重新运行目标测试验证通过**

运行：
```bash
cargo test --test test_load_spec load_spec_lfm2_5_embedding_auto_resolves_to_safetensors_default -- --exact
cargo test --test test_load_spec load_spec_lfm2_5_embedding_rejects_onnx -- --exact
```

预期：PASS，两条测试都报告 `ok`。

- [ ] **步骤 5：做 diff review 检查点**

运行：
```bash
git diff -- src/models/mod.rs src/models/core/artifact.rs tests/test_load_spec.rs
```

预期：diff 只包含新模型元信息与 artifact 规则，不应出现无关模型行为改动。

## 任务 2：引入统一 embedding prompt 选项并打通 CLI / API 解析

**文件：**
- 修改：`src/models/mod.rs`
- 修改：`src/main.rs`
- 修改：`src/api/mod.rs`
- 测试：`src/main.rs`
- 测试：`src/api/mod.rs`

- [ ] **步骤 1：先补 CLI 与 API 的失败测试**

```rust
// src/main.rs tests
#[test]
fn parse_run_lfm2_5_embedding_prompt_name_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "run",
        "--model",
        "lfm2.5-embedding-350m",
        "--input",
        "capital of france",
        "--weight-path",
        "D:\\model_download\\LFM2.5-Embedding-350M",
        "--prompt-name",
        "query",
    ])
    .expect("run args should parse");

    let Some(Commands::Run(args)) = cli.command else {
        panic!("expected run subcommand");
    };
    assert_eq!(args.model, WhichModel::LFM2_5Embedding350M);
    assert!(matches!(args.prompt_name, Some(EmbeddingPromptArg::Query)));
}
```

```rust
// src/api/mod.rs tests
#[test]
fn test_parse_embedding_prompt_name_defaults_to_document() {
    let prompt = parse_embedding_prompt_name(None).unwrap();
    assert_eq!(prompt, EmbeddingPromptName::Document);
}

#[test]
fn test_parse_embedding_prompt_name_accepts_query() {
    let prompt = parse_embedding_prompt_name(Some("query")).unwrap();
    assert_eq!(prompt, EmbeddingPromptName::Query);
}

#[test]
fn test_parse_embedding_prompt_name_rejects_unknown_value() {
    let err = parse_embedding_prompt_name(Some("title")).unwrap_err().to_string();
    assert!(err.contains("unsupported embedding prompt_name"));
}
```

- [ ] **步骤 2：运行一个精确测试确认失败**

运行：
```bash
cargo test parse_run_lfm2_5_embedding_prompt_name_flags -- --exact
```

预期：FAIL，编译错误提示 `EmbeddingPromptArg` 或 `prompt_name` 字段不存在。

- [ ] **步骤 3：实现统一 prompt 类型、CLI 参数与 API 解析**

```rust
// src/models/mod.rs
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum EmbeddingPromptName {
    Query,
    #[default]
    Document,
}

impl EmbeddingPromptName {
    pub fn parse_api_str(value: &str) -> Result<Self> {
        match value {
            "query" => Ok(Self::Query),
            "document" => Ok(Self::Document),
            other => Err(anyhow!("unsupported embedding prompt_name: {other}")),
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub struct EmbeddingOptions {
    pub prompt_name: EmbeddingPromptName,
}
```

```rust
// src/main.rs
#[derive(Clone, Copy, Debug, PartialEq, Eq, ValueEnum)]
enum EmbeddingPromptArg {
    Query,
    Document,
}

impl From<EmbeddingPromptArg> for aha::models::EmbeddingPromptName {
    fn from(value: EmbeddingPromptArg) -> Self {
        match value {
            EmbeddingPromptArg::Query => Self::Query,
            EmbeddingPromptArg::Document => Self::Document,
        }
    }
}

#[derive(Args, Debug)]
struct RunArgs {
    #[arg(short, long, num_args = 1..=2, value_delimiter = ' ', required_unless_present = "request_json")]
    input: Vec<String>,

    #[arg(long)]
    request_json: Option<String>,

    #[arg(long, value_enum)]
    prompt_name: Option<EmbeddingPromptArg>,
}
```

```rust
// src/api/mod.rs
#[derive(Debug, Deserialize)]
pub(crate) struct EmbeddingRequest {
    pub model: Option<String>,
    pub input: Value,
    pub prompt_name: Option<String>,
}

fn parse_embedding_prompt_name(value: Option<&str>) -> anyhow::Result<EmbeddingPromptName> {
    match value {
        None => Ok(EmbeddingPromptName::Document),
        Some(value) => EmbeddingPromptName::parse_api_str(value),
    }
}
```

- [ ] **步骤 4：运行针对性测试确认通过**

运行：
```bash
cargo test parse_run_lfm2_5_embedding_prompt_name_flags -- --exact
cargo test test_parse_embedding_prompt_name_defaults_to_document -- --exact
cargo test test_parse_embedding_prompt_name_accepts_query -- --exact
cargo test test_parse_embedding_prompt_name_rejects_unknown_value -- --exact
```

预期：PASS；CLI parse 能拿到 `prompt_name`，API helper 对默认值、`query`、非法值均有稳定结果。

- [ ] **步骤 5：做 diff review 检查点**

运行：
```bash
git diff -- src/models/mod.rs src/main.rs src/api/mod.rs
```

预期：diff 只引入 prompt 类型、参数解析与 `/embeddings` 透传，不应改变 chat、rerank 或 OCR 路径。

## 任务 3：实现 `lfm2_5_embedding` 配置解析与结构校验

**文件：**
- 创建：`src/models/lfm2_5_embedding/mod.rs`
- 创建：`src/models/lfm2_5_embedding/config.rs`
- 测试：`src/models/lfm2_5_embedding/config.rs`

- [ ] **步骤 1：在 `config.rs` 写失败的纯单元测试**

```rust
#[cfg(test)]
mod tests {
    use super::{Lfm2_5EmbeddingConfig, resolve_pooling, resolve_prompts};
    use std::collections::HashMap;

    #[test]
    fn resolve_prompts_requires_query_and_document() {
        let mut prompts = HashMap::new();
        prompts.insert("query".to_string(), "query: ".to_string());
        let err = resolve_prompts(prompts).unwrap_err().to_string();
        assert!(err.contains("document"));
    }

    #[test]
    fn resolve_pooling_rejects_non_cls_layout() {
        let err = resolve_pooling(false, true, false, false).unwrap_err().to_string();
        assert!(err.contains("CLS"));
    }

    #[test]
    fn normalize_defaults_to_true_for_cosine_similarity() {
        assert!(Lfm2_5EmbeddingConfig::normalize_from_similarity(Some("cosine")));
        assert!(!Lfm2_5EmbeddingConfig::normalize_from_similarity(Some("dot")));
        assert!(!Lfm2_5EmbeddingConfig::normalize_from_similarity(None));
    }
}
```

- [ ] **步骤 2：运行一个精确测试确认失败**

运行：
```bash
cargo test resolve_prompts_requires_query_and_document -- --exact
```

预期：FAIL，编译错误提示 `src/models/lfm2_5_embedding/config.rs` 或相应 helper 不存在。

- [ ] **步骤 3：实现配置结构与校验 helper**

```rust
// src/models/lfm2_5_embedding/mod.rs
pub mod config;
pub mod generate;
pub mod model;
```

```rust
// src/models/lfm2_5_embedding/config.rs
use std::{collections::HashMap, path::Path};

use anyhow::{Result, anyhow};
use serde::Deserialize;

use crate::models::lfm2_5::config::Lfm2_5Config;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Lfm2_5EmbeddingPoolingStrategy {
    Cls,
}

#[derive(Debug, Clone)]
pub struct Lfm2_5EmbeddingConfig {
    pub base: Lfm2_5Config,
    pub prompts: HashMap<String, String>,
    pub pooling: Lfm2_5EmbeddingPoolingStrategy,
    pub normalize: bool,
    pub word_embedding_dimension: usize,
}

#[derive(Debug, Deserialize)]
struct SentenceTransformersConfig {
    #[serde(default)]
    prompts: HashMap<String, String>,
    similarity_fn_name: Option<String>,
}

#[derive(Debug, Deserialize)]
struct PoolingConfig {
    #[serde(default)]
    word_embedding_dimension: usize,
    #[serde(default)]
    pooling_mode_cls_token: bool,
    #[serde(default)]
    pooling_mode_mean_tokens: bool,
    #[serde(default)]
    pooling_mode_max_tokens: bool,
    #[serde(default)]
    pooling_mode_mean_sqrt_len_tokens: bool,
}

pub fn resolve_prompts(mut prompts: HashMap<String, String>) -> Result<HashMap<String, String>> {
    let query = prompts.remove("query");
    let document = prompts.remove("document");
    match (query, document) {
        (Some(query), Some(document)) => Ok(HashMap::from([
            ("query".to_string(), query),
            ("document".to_string(), document),
        ])),
        (None, _) => Err(anyhow!("missing query prompt in config_sentence_transformers.json")),
        (_, None) => Err(anyhow!("missing document prompt in config_sentence_transformers.json")),
    }
}

pub fn resolve_pooling(
    cls: bool,
    mean: bool,
    max: bool,
    mean_sqrt_len: bool,
) -> Result<Lfm2_5EmbeddingPoolingStrategy> {
    if cls && !mean && !max && !mean_sqrt_len {
        return Ok(Lfm2_5EmbeddingPoolingStrategy::Cls);
    }
    Err(anyhow!(
        "LFM2.5-Embedding-350M currently supports only CLS pooling"
    ))
}

impl Lfm2_5EmbeddingConfig {
    pub fn normalize_from_similarity(value: Option<&str>) -> bool {
        matches!(value, Some("cosine"))
    }

    pub fn load(path: &str) -> Result<Self> {
        let base_path = Path::new(path).join("config.json");
        let base: Lfm2_5Config = serde_json::from_slice(&std::fs::read(base_path)?)?;

        if !base.architectures.iter().any(|name| name == "Lfm2BidirectionalModel") {
            return Err(anyhow!("config.json does not declare Lfm2BidirectionalModel"));
        }

        let st_path = Path::new(path).join("config_sentence_transformers.json");
        let st: SentenceTransformersConfig = serde_json::from_slice(&std::fs::read(st_path)?)?;

        let pooling_path = Path::new(path).join("1_Pooling").join("config.json");
        let pooling_cfg: PoolingConfig =
            serde_json::from_slice(&std::fs::read(pooling_path)?)?;

        let prompts = resolve_prompts(st.prompts)?;
        let pooling = resolve_pooling(
            pooling_cfg.pooling_mode_cls_token,
            pooling_cfg.pooling_mode_mean_tokens,
            pooling_cfg.pooling_mode_max_tokens,
            pooling_cfg.pooling_mode_mean_sqrt_len_tokens,
        )?;

        if pooling_cfg.word_embedding_dimension != base.hidden_size {
            return Err(anyhow!(
                "pooling word_embedding_dimension {} does not match hidden_size {}",
                pooling_cfg.word_embedding_dimension,
                base.hidden_size
            ));
        }

        Ok(Self {
            base,
            prompts,
            pooling,
            normalize: Self::normalize_from_similarity(st.similarity_fn_name.as_deref()),
            word_embedding_dimension: pooling_cfg.word_embedding_dimension,
        })
    }
}
```

- [ ] **步骤 4：重新运行配置单测验证通过**

运行：
```bash
cargo test resolve_prompts_requires_query_and_document -- --exact
cargo test resolve_pooling_rejects_non_cls_layout -- --exact
cargo test normalize_defaults_to_true_for_cosine_similarity -- --exact
```

预期：PASS，说明 prompt、pooling、normalize 三个关键配置决策都已固定。

- [ ] **步骤 5：做 diff review 检查点**

运行：
```bash
git diff -- src/models/lfm2_5_embedding/mod.rs src/models/lfm2_5_embedding/config.rs
```

预期：diff 只包含 family 导出与配置解析，不应混入 runtime 前向实现。

## 任务 4：实现双向 embedding backend、生成包装器、registry 与 API handler 接入

**文件：**
- 创建：`src/models/lfm2_5_embedding/model.rs`
- 创建：`src/models/lfm2_5_embedding/generate.rs`
- 修改：`src/models/mod.rs`
- 修改：`src/models/core/registry.rs`
- 修改：`src/api/mod.rs`

- [ ] **步骤 1：先在新集成测试文件里写失败测试骨架**

```rust
#[test]
fn lfm2_5_embedding_safetensors_init_from_spec_can_embed() -> Result<()> {
    let spec = LoadSpec {
        model: WhichModel::LFM2_5Embedding350M,
        artifact: ArtifactKind::Safetensors,
        paths: ModelPaths {
            weight_dir: Some(LFM2_5_EMBEDDING_DIR.to_string()),
            ..Default::default()
        },
    };

    let mut model = Lfm2_5EmbeddingModel::init_from_spec(&spec, None, None)?;
    let output = model.embed_with_options(
        &["test safetensors embedding".to_string()],
        EmbeddingOptions {
            prompt_name: EmbeddingPromptName::Document,
        },
    )?;

    assert_eq!(output.len(), 1);
    assert_eq!(output[0].len(), 1024);
    Ok(())
}
```

- [ ] **步骤 2：运行该测试确认当前失败**

运行：
```bash
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_safetensors_init_from_spec_can_embed -- --exact --nocapture
```

预期：FAIL，编译错误提示 `Lfm2_5EmbeddingModel` / `test_lfm2_5_embedding_safetensors.rs` 尚不存在。

- [ ] **步骤 3：实现 backend、生成包装器与 registry 分发**

先在 `src/models/lfm2_5_embedding/model.rs` 中，直接复制并重命名 `src/models/lfm2_5/model.rs` 的私有 `Lfm2_5MLP`、`Lfm2_5Attention`、`Lfm2_5ShortConv`、`Lfm2_5DecoderLayer`，分别命名为 `Lfm2_5EmbeddingMLP`、`Lfm2_5EmbeddingAttention`、`Lfm2_5EmbeddingShortConv`、`Lfm2_5BidirectionalLayer`。唯一行为修改是：双向前向不构造 causal attention mask，也不引入 `lm_head`。

```rust
// src/models/lfm2_5_embedding/model.rs
use anyhow::{Result, anyhow};
use candle_core::{DType, Device, IndexOp, Tensor};
use candle_nn::{Embedding, Linear, Module, RmsNorm, VarBuilder, embedding, linear_no_bias, rms_norm};

use crate::{
    models::{
        EmbeddingOptions, EmbeddingPromptName,
        lfm2_5::config::Lfm2_5Config,
        lfm2_5_embedding::config::{Lfm2_5EmbeddingConfig, Lfm2_5EmbeddingPoolingStrategy},
    },
    position_embed::rope::{RoPE, apply_rotary_pos_emb},
    tokenizer::TokenizerModel,
    utils::{find_type_files, get_device, get_dtype},
};

pub struct Lfm2_5BidirectionalModel {
    embed_tokens: Embedding,
    layers: Vec<Lfm2_5BidirectionalLayer>,
    embedding_norm: RmsNorm,
    rotary_emb: RoPE,
}

impl Lfm2_5BidirectionalModel {
    pub fn new_from_vb(vb: VarBuilder, cfg: &Lfm2_5Config) -> Result<Self> {
        let embed_tokens = embedding(cfg.vocab_size, cfg.hidden_size, vb.pp("model").pp("embed_tokens"))?;
        let embedding_norm = rms_norm(cfg.hidden_size, cfg.norm_eps, vb.pp("model").pp("embedding_norm"))?;
        let rotary_emb = RoPE::new(cfg.head_dim(), cfg.rope_parameters.rope_theta, vb.device())?;
        let layer_kinds = crate::models::lfm2_5::model::validate_layer_types(&cfg.layer_types)?;
        let vb_layers = vb.pp("model").pp("layers");
        let layers = layer_kinds
            .into_iter()
            .enumerate()
            .map(|(idx, kind)| Lfm2_5BidirectionalLayer::new_from_vb(vb_layers.pp(idx), cfg, kind))
            .collect::<Result<Vec<_>>>()?;
        Ok(Self {
            embed_tokens,
            layers,
            embedding_norm,
            rotary_emb,
        })
    }

    pub fn forward_hidden(&mut self, input_ids: &Tensor) -> Result<Tensor> {
        let mut hidden_states = self.embed_tokens.forward(input_ids)?;
        let (_, seq_len, _) = hidden_states.dims3()?;
        let (cos, sin) = self.rotary_emb.forward(0, seq_len, hidden_states.device())?;
        for layer in self.layers.iter_mut() {
            hidden_states = layer.forward(&hidden_states, &cos, &sin, None)?;
        }
        self.embedding_norm.forward(&hidden_states)
    }
}

fn l2_normalize(values: &mut [f32]) {
    let norm = values.iter().map(|v| v * v).sum::<f32>().sqrt();
    if norm > 0.0 {
        for value in values.iter_mut() {
            *value /= norm;
        }
    }
}

pub struct Lfm2_5EmbeddingSafetensorsBackend {
    tokenizer: TokenizerModel,
    model: Lfm2_5BidirectionalModel,
    device: Device,
    prompts: std::collections::HashMap<String, String>,
    pooling: Lfm2_5EmbeddingPoolingStrategy,
    normalize: bool,
}

impl Lfm2_5EmbeddingSafetensorsBackend {
    pub fn load(path: &str, device: Option<&Device>, dtype: Option<DType>) -> Result<Self> {
        let cfg = Lfm2_5EmbeddingConfig::load(path)?;
        let tokenizer = TokenizerModel::init(path)?;
        let device = get_device(device);
        let dtype = get_dtype(dtype, cfg.base.dtype.as_str());
        let model_list = find_type_files(path, "safetensors")?;
        let vb = unsafe { VarBuilder::from_mmaped_safetensors(&model_list, dtype, &device)? };
        let model = Lfm2_5BidirectionalModel::new_from_vb(vb, &cfg.base)?;

        Ok(Self {
            tokenizer,
            model,
            device,
            prompts: cfg.prompts,
            pooling: cfg.pooling,
            normalize: cfg.normalize,
        })
    }

    pub fn embed_texts(
        &mut self,
        input: &[String],
        options: EmbeddingOptions,
    ) -> Result<Vec<Vec<f32>>> {
        if input.is_empty() {
            return Err(anyhow!("embedding input cannot be empty"));
        }

        input
            .iter()
            .map(|text| self.embed_one(text, options.prompt_name))
            .collect()
    }

    fn embed_one(&mut self, text: &str, prompt_name: EmbeddingPromptName) -> Result<Vec<f32>> {
        let prompt_key = match prompt_name {
            EmbeddingPromptName::Query => "query",
            EmbeddingPromptName::Document => "document",
        };
        let prompt = self
            .prompts
            .get(prompt_key)
            .ok_or_else(|| anyhow!("missing prompt mapping for {prompt_key}"))?;
        let rendered = format!("{prompt}{text}");
        let input_ids = self.tokenizer.text_encode(rendered, &self.device)?;
        let hidden = self.model.forward_hidden(&input_ids)?.squeeze(0)?.to_dtype(DType::F32)?;
        let mut pooled = match self.pooling {
            Lfm2_5EmbeddingPoolingStrategy::Cls => hidden.i(0)?.to_vec1::<f32>()?,
        };
        if self.normalize {
            l2_normalize(&mut pooled);
        }
        Ok(pooled)
    }
}
```

```rust
// src/models/lfm2_5_embedding/generate.rs
use anyhow::Result;
use candle_core::{DType, Device};

use crate::models::{
    EmbeddingOptions,
    artifact::{ArtifactKind, LoadSpec},
    lfm2_5_embedding::model::Lfm2_5EmbeddingSafetensorsBackend,
};

pub struct Lfm2_5EmbeddingModel {
    backend: Lfm2_5EmbeddingSafetensorsBackend,
}

impl Lfm2_5EmbeddingModel {
    pub fn init_from_spec(
        spec: &LoadSpec,
        device: Option<&Device>,
        dtype: Option<DType>,
    ) -> Result<Self> {
        match spec.resolved_artifact() {
            ArtifactKind::Safetensors => {
                let path = spec.paths.weight_dir.as_deref().ok_or_else(|| {
                    anyhow::anyhow!("weight_path is required for lfm2.5-embedding-350m safetensors")
                })?;
                Self::init(path, device, dtype)
            }
            ArtifactKind::Gguf => Err(anyhow::anyhow!(
                "lfm2.5-embedding-350m gguf runtime is not implemented"
            )),
            ArtifactKind::Onnx => Err(anyhow::anyhow!(
                "lfm2.5-embedding-350m onnx runtime is not implemented"
            )),
            ArtifactKind::Auto => unreachable!("artifact kind should be resolved before init"),
        }
    }

    pub fn init(path: &str, device: Option<&Device>, dtype: Option<DType>) -> Result<Self> {
        let backend = Lfm2_5EmbeddingSafetensorsBackend::load(path, device, dtype)?;
        Ok(Self { backend })
    }

    pub fn embed_with_options(
        &mut self,
        input: &[String],
        options: EmbeddingOptions,
    ) -> Result<Vec<Vec<f32>>> {
        self.backend.embed_texts(input, options)
    }
}
```

```rust
// src/models/mod.rs - add the new variant and switch embedding to options-aware dispatch
pub mod lfm2_5_embedding;
LFM2_5Embedding(lfm2_5_embedding::generate::Lfm2_5EmbeddingModel),

pub fn embedding(
    &mut self,
    input: &[String],
    options: &EmbeddingOptions,
) -> Result<Vec<Vec<f32>>> {
    match self {
        ModelInstance::AllMiniLML6V2(model) => model.embed(input),
        ModelInstance::Qwen3Embedding(model) => model.embed(input),
        ModelInstance::LFM2_5Embedding(model) => model.embed_with_options(input, *options),
        _ => Err(anyhow!("current model does not support embeddings")),
    }
}
```

```rust
// src/models/core/registry.rs - add only the new family arm
LFM2_5Embedding,
WhichModel::LFM2_5Embedding350M => ModelLoaderFamily::LFM2_5Embedding,

ModelLoaderFamily::LFM2_5Embedding => ModelInstance::LFM2_5Embedding(
    Lfm2_5EmbeddingModel::init_from_spec(spec, None, None)?,
),
```

```rust
// src/api/mod.rs - wire prompt_name into the existing handler
let prompt_name = match parse_embedding_prompt_name(req.prompt_name.as_deref()) {
    Ok(value) => value,
    Err(e) => {
        return (
            Status::BadRequest,
            Json(serde_json::json!({ "error": e.to_string() })),
        );
    }
};

let options = EmbeddingOptions { prompt_name };
let embeddings = match guard.instance.embedding(&texts, &options) {
    Ok(value) => value,
    Err(e) => {
        return (
            Status::BadRequest,
            Json(serde_json::json!({ "error": e.to_string() })),
        );
    }
};
```

- [ ] **步骤 4：重新运行精确测试确认 backend 可加载并输出 1024 维**

运行：
```bash
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_safetensors_init_from_spec_can_embed -- --exact --nocapture
```

预期：PASS，输出 `assert_eq!(output[0].len(), 1024)` 通过。

- [ ] **步骤 5：做 diff review 检查点**

运行：
```bash
git diff -- src/models/lfm2_5_embedding/model.rs src/models/lfm2_5_embedding/generate.rs src/models/mod.rs src/models/core/registry.rs src/api/mod.rs
```

预期：diff 只包含新 family 的 backend、registry 与 `/embeddings` handler 接入，不应改动现有 `lfm2_5` 生成前向。

## 任务 5：接入 CLI exec、`run_target_model_with_spec` 与本地集成测试

**文件：**
- 创建：`src/exec/lfm2_5_embedding.rs`
- 修改：`src/exec/mod.rs`
- 修改：`src/main.rs`
- 创建：`tests/test_lfm2_5_embedding_safetensors.rs`

- [ ] **步骤 1：在新测试文件中补齐失败的本地集成测试**

```rust
use std::path::Path;

use aha::models::{
    ArtifactKind, EmbeddingOptions, EmbeddingPromptName, LoadSpec, ModelPaths, WhichModel,
    common::retrieval::cosine_similarity,
    lfm2_5_embedding::generate::Lfm2_5EmbeddingModel,
};
use anyhow::{Result, anyhow};

const LFM2_5_EMBEDDING_DIR: &str = r"D:\model_download\LFM2.5-Embedding-350M";

fn require_existing_dir(path: &str) -> Result<()> {
    let dir = Path::new(path);
    if !dir.exists() {
        return Err(anyhow!("model dir not found: {}", path));
    }
    if !dir.is_dir() {
        return Err(anyhow!("path is not a directory: {}", path));
    }
    Ok(())
}

#[test]
fn lfm2_5_embedding_safetensors_can_load() -> Result<()> {
    require_existing_dir(LFM2_5_EMBEDDING_DIR)?;
    let _model = Lfm2_5EmbeddingModel::init(LFM2_5_EMBEDDING_DIR, None, None)?;
    Ok(())
}

#[test]
fn lfm2_5_embedding_query_and_document_prompts_differ() -> Result<()> {
    require_existing_dir(LFM2_5_EMBEDDING_DIR)?;
    let mut model = Lfm2_5EmbeddingModel::init(LFM2_5_EMBEDDING_DIR, None, None)?;

    let text = "capital of france".to_string();
    let query = model.embed_with_options(
        &[text.clone()],
        EmbeddingOptions {
            prompt_name: EmbeddingPromptName::Query,
        },
    )?;
    let document = model.embed_with_options(
        &[text],
        EmbeddingOptions {
            prompt_name: EmbeddingPromptName::Document,
        },
    )?;

    assert_ne!(query[0], document[0]);
    Ok(())
}

#[test]
fn lfm2_5_embedding_query_matches_relevant_document() -> Result<()> {
    require_existing_dir(LFM2_5_EMBEDDING_DIR)?;
    let mut model = Lfm2_5EmbeddingModel::init(LFM2_5_EMBEDDING_DIR, None, None)?;

    let query = model.embed_with_options(
        &["What is the capital of France?".to_string()],
        EmbeddingOptions {
            prompt_name: EmbeddingPromptName::Query,
        },
    )?;
    let docs = model.embed_with_options(
        &[
            "Paris is the capital and largest city of France.".to_string(),
            "Saturn is the sixth planet from the Sun.".to_string(),
        ],
        EmbeddingOptions {
            prompt_name: EmbeddingPromptName::Document,
        },
    )?;

    let positive = cosine_similarity(&query[0], &docs[0]);
    let negative = cosine_similarity(&query[0], &docs[1]);
    assert!(positive > negative, "expected positive {positive} > negative {negative}");
    Ok(())
}
```

- [ ] **步骤 2：运行一个精确测试确认当前失败**

运行：
```bash
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_query_and_document_prompts_differ -- --exact --nocapture
```

预期：FAIL，编译错误提示 `Lfm2_5EmbeddingModel::embed_with_options` 或 CLI exec 分支尚未完整接上。

- [ ] **步骤 3：实现 CLI exec 与 `run_target_model_with_spec` 分发**

```rust
// src/exec/lfm2_5_embedding.rs
use anyhow::Result;

use crate::models::{
    EmbeddingOptions, LoadSpec,
    lfm2_5_embedding::generate::Lfm2_5EmbeddingModel,
};
use crate::utils::get_file_path;

pub struct Lfm2_5EmbeddingExec;

impl Lfm2_5EmbeddingExec {
    pub fn run_with_spec(
        input: &[String],
        output: Option<&str>,
        spec: &LoadSpec,
        options: EmbeddingOptions,
    ) -> Result<()> {
        let input_text = input
            .first()
            .ok_or_else(|| anyhow::anyhow!("embedding run requires one text input"))?;
        let text = if input_text.starts_with("file://") {
            let path = get_file_path(input_text)?;
            std::fs::read_to_string(path)?
        } else {
            input_text.clone()
        };

        let mut model = Lfm2_5EmbeddingModel::init_from_spec(spec, None, None)?;
        let embedding = model.embed_with_options(&[text], options)?;

        let output_json = serde_json::to_string_pretty(&embedding)?;
        println!("{}", output_json);
        if let Some(out) = output {
            std::fs::write(out, output_json)?;
            println!("Output saved to: {}", out);
        }
        Ok(())
    }
}
```

```rust
// src/exec/mod.rs
pub mod lfm2_5_embedding;
```

```rust
// src/main.rs - insert a new branch into the existing match
WhichModel::LFM2_5Embedding350M => {
    use aha::exec::lfm2_5_embedding::Lfm2_5EmbeddingExec;
    let options = aha::models::EmbeddingOptions {
        prompt_name: args.prompt_name.unwrap_or(EmbeddingPromptArg::Document).into(),
    };
    Lfm2_5EmbeddingExec::run_with_spec(&args.input, args.output.as_deref(), spec, options)?;
    Ok(true)
}

#[test]
fn parse_run_lfm2_5_embedding_weight_path_flags() {
    let cli = Cli::try_parse_from([
        "aha",
        "run",
        "--model",
        "lfm2.5-embedding-350m",
        "--input",
        "hello",
        "--weight-path",
        "D:\\model_download\\LFM2.5-Embedding-350M",
    ])
    .expect("run args should parse");

    let Some(Commands::Run(args)) = cli.command else {
        panic!("expected run subcommand");
    };
    assert_eq!(args.model, WhichModel::LFM2_5Embedding350M);
    assert_eq!(
        args.weight_path.as_deref(),
        Some("D:\\model_download\\LFM2.5-Embedding-350M")
    );
}
```

- [ ] **步骤 4：运行本地集成测试与 CLI parse 测试确认通过**

运行：
```bash
cargo test parse_run_lfm2_5_embedding_weight_path_flags -- --exact
cargo test parse_run_lfm2_5_embedding_prompt_name_flags -- --exact
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_safetensors_can_load -- --exact --nocapture
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_query_and_document_prompts_differ -- --exact --nocapture
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_query_matches_relevant_document -- --exact --nocapture
```

预期：PASS；本地模型目录存在时，三条集成测试均通过。

- [ ] **步骤 5：做 diff review 检查点**

运行：
```bash
git diff -- src/exec/mod.rs src/exec/lfm2_5_embedding.rs src/main.rs tests/test_lfm2_5_embedding_safetensors.rs
```

预期：diff 只包含新 embedding exec、CLI 分发与新测试文件，不应影响现有 `all-minilm`、`qwen3-embedding` 的运行路径。

## 任务 6：补文档并做最终验证

**文件：**
- 修改：`README.md`
- 修改：`README.zh-CN.md`
- 修改：`docs/supported-models.md`
- 修改：`docs/supported-models.zh-CN.md`

- [ ] **步骤 1：先补文档示例与支持列表变更**

```md
# README.md
aha run -m lfm2.5-embedding-350m -i "Rust embedding test" --weight-path D:\model_download\LFM2.5-Embedding-350M

aha run -m lfm2.5-embedding-350m -i "What is the capital of France?" --weight-path D:\model_download\LFM2.5-Embedding-350M --prompt-name query

curl http://localhost:10100/embeddings \
  -H "Content-Type: application/json" \
  -d '{
    "model": "lfm2.5-embedding-350m",
    "input": "What is the capital of France?",
    "prompt_name": "query"
  }'
```

```md
# docs/supported-models.md
| **Embedding** | Qwen3-Embedding, all-MiniLM-L6-v2, LFM2.5-Embedding-350M |
```

```md
# README.zh-CN.md
aha run -m lfm2.5-embedding-350m -i "Rust embedding test" --weight-path D:\model_download\LFM2.5-Embedding-350M

aha run -m lfm2.5-embedding-350m -i "法国首都是哪里？" --weight-path D:\model_download\LFM2.5-Embedding-350M --prompt-name query
```

- [ ] **步骤 2：运行最终验证命令**

运行：
```bash
cargo test --test test_load_spec load_spec_lfm2_5_embedding_auto_resolves_to_safetensors_default -- --exact
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_safetensors_init_from_spec_can_embed -- --exact --nocapture
cargo test --test test_lfm2_5_embedding_safetensors lfm2_5_embedding_query_matches_relevant_document -- --exact --nocapture
cargo check -q
```

预期：所有测试 PASS，`cargo check -q` 无编译错误。

- [ ] **步骤 3：做最终 diff review 检查点**

运行：
```bash
git diff -- README.md README.zh-CN.md docs/supported-models.md docs/supported-models.zh-CN.md
git diff -- src/main.rs src/api/mod.rs src/models/mod.rs src/models/core/artifact.rs src/models/core/registry.rs src/models/lfm2_5_embedding/mod.rs src/models/lfm2_5_embedding/config.rs src/models/lfm2_5_embedding/model.rs src/models/lfm2_5_embedding/generate.rs src/exec/mod.rs src/exec/lfm2_5_embedding.rs tests/test_load_spec.rs tests/test_lfm2_5_embedding_safetensors.rs
```

预期：第一条 diff 只包含文档示例和支持列表，第二条 diff 只包含本计划列出的实现文件。

## 规格覆盖检查

- 新模型 ID `lfm2.5-embedding-350m`：任务 1、任务 4、任务 5。
- 仅支持 safetensors：任务 1、任务 4、任务 6。
- `prompt_name=query|document`：任务 2、任务 4、任务 5。
- 不复用现有 `lfm2.5-350m` 生成路径：任务 4、任务 5。
- `/embeddings` 接口支持：任务 2、任务 4。
- CLI `run` 支持：任务 2、任务 5。
- 测试覆盖与文档：任务 5、任务 6。
